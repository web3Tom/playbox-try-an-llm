# Sample Todo API

A tiny in-memory Todo REST API used as the bundled target for the Polestar
Playbox codebase analyzer. It has just enough structure — a clear entrypoint,
an API layer, a service layer, a model, and a storage repository — to produce an
interesting architecture graph.

Not a real service; it exists so the analyzer has something to chew on without
any credentials or network access.
