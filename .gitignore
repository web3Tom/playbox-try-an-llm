# Secrets — never commit
.env

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Node
node_modules/

# Build / generated artifacts (patterns must stay comment-free: '#' is only a
# comment at the start of a line, so an inline comment breaks the pattern)
site/
dist/
*.egg-info/

# OS / editor
.DS_Store
